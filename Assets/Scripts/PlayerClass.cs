public enum PlayerClass
{
    Fighter, // Uses Strength/Fury as primary stat
    Wizard, // Uses Intelligence/Wisdom as primary stat
    Scout, // Uses Agility/Endurance as primary stat
    Ranger, // Uses Agility/Wisdom as primary stat
    Cleric // Uses Faith/Wisdom as primary stat 
           // In the future I'd love to add dual-classes- a fighter trains as a cleric and becomes a PALADIN, 
           // utilizing strength & faith and using wisdom instead of fury.
           // Add more classes as you envision them
}